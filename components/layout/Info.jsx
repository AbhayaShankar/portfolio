import React from "react";
import classes from "./Info.module.css";

const Info = () => {
  return (
    <div className={classes.info}>
      <button className={classes.btn}>i</button>
    </div>
  );
};

export default Info;
